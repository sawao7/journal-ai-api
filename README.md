# for API
## Request format

{
     "inputs": {
         "role": "string",
         "journal": "string",
         "goal": "string",
         "progress": "string",
    }
}
## Output format
{"output":"string"}

# install
python version: ^3.9
```
poetry install
```

# use
```
poetry run python main.py
```
